%
#
# Template to generate an acknowledge sequence for a no CTS UART
# that is UART4 and 5.
# 

input(it,obj)

# Only the LIN break may be ack automatically

let uartName := [it::NAME leftSubString: 5]

if exists obj::SOURCE_S then
  foreach ev in obj::SOURCE_S::EVFLAG do
    if ev::VALUE == "LBD" then
%
  /* Clear the LBD flag */
  % !uartName %->SR = ~(1U << 8) & 0x3FFU;%
    end if
  end foreach
end if